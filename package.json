{
  "name": "@lleverage-ai/agent-sdk-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '@lleverage-ai/agent-stream' build && bun run --filter '@lleverage-ai/agent-ledger' build && bun run --filter '@lleverage-ai/agent-sdk' build",
    "test": "bun run --filter '@lleverage-ai/agent-stream' build && bun run --filter '@lleverage-ai/agent-stream' test && bun run --filter '@lleverage-ai/agent-ledger' test && bun run --filter '@lleverage-ai/agent-sdk' test",
    "type-check": "bun run --filter '@lleverage-ai/agent-stream' build && bun run --filter '@lleverage-ai/agent-stream' type-check && bun run --filter '@lleverage-ai/agent-ledger' type-check && bun run --filter '@lleverage-ai/agent-sdk' type-check",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "format": "biome format --write .",
    "clean": "bun run --filter '@lleverage-ai/agent-sdk' clean && bun run --filter '@lleverage-ai/agent-ledger' clean && bun run --filter '@lleverage-ai/agent-stream' clean",
    "prepare": "simple-git-hooks"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.14",
    "@tsconfig/node18": "^18.2.4",
    "@types/json-schema": "^7.0.15",
    "@types/node": "^25.1.0",
    "simple-git-hooks": "^2.11.1",
    "typescript": "^5.9.2"
  },
  "simple-git-hooks": {
    "pre-push": "bun run check && bun run test"
  }
}
